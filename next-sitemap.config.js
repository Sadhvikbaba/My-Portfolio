/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://my-portfolio-khaki-two-52.vercel.app',
  generateRobotsTxt: true,
  sitemapSize: 7000,
  changefreq: 'weekly',
  priority: 0.7,
};

module.exports = config;
